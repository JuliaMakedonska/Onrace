# Concept

Visual direction exploration — moodboards, style tiles, early high-fidelity concepts for Onrace's "bold & competitive" brand tone (see `../CLAUDE.md`). Feeds into `../tokens/` and `../design-system/` once a direction is chosen. Empty for now.
