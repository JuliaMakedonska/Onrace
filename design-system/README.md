# Design system

The assembled system: `../tokens/` + `../components/` documented as a coherent whole — usage guidelines, patterns, do's/don'ts. The reference for anyone implementing UI. Empty for now.
