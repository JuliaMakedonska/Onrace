# AllTrails — screenshot unavailable

No browser automation tool was available in this session (Claude in Chrome was declined; no Playwright install found). AllTrails' public trail map/directory has no login wall; the personal "Completed" activity log is account-gated — **access restricted** for that part specifically.

Notes gathered via WebFetch, see `../research.md` → Competitor grouping / deep-dive comparison table.

**To capture later**: public trail map + filter panel (no login needed), personal "Completed" log map view (needs a logged-in account).
