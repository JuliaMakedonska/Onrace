# RoxConnect — screenshot unavailable

No browser automation tool was available in this session (Claude in Chrome was declined; no Playwright install found). RoxConnect is primarily a native mobile app (iOS/Android) — its marketing site was fetched via WebFetch instead, but full in-app screens (profile, matching, event chat) are not reachable without the app itself or a login.

Notes gathered via WebFetch, see `../research.md` → Competitor grouping / deep-dive comparison table.

**To capture later**: marketing site hero/feature sections at minimum; in-app screens would need the mobile app installed.
