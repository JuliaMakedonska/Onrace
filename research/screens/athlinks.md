# Athlinks — screenshot unavailable

No browser automation tool was available in this session (Claude in Chrome was declined; no Playwright install found). Athlinks' homepage also returned no usable content via WebFetch (page likely client-rendered); the personal race-history/claim flow additionally sits behind a login — **access restricted** for that part regardless of tooling.

Notes gathered via WebSearch (Zendesk help docs, Wikipedia, third-party reviews), see `../research.md` → Competitor grouping / deep-dive comparison table.

**To capture later**: race search/results list (public), claimed-results personal history page (needs a logged-in account).
